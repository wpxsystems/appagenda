node_modules
dist
.next
.turbo
*.env
*.env.local
.DS_Store
coverage
*.tsbuildinfo

# IDE configs (pessoais — nunca commitar)
.idea/
.vscode/
*.iml

# Expo / React Native gerados (regeneram via `npx expo prebuild`)
.expo/
apps/mobile/android/
apps/mobile/ios/

# Credenciais e backups — nunca commitar
docs/credenciais.md
apps/_api_fastify_backup/
